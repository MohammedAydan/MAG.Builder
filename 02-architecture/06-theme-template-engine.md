# Theme and Template Engine

Themes provide design tokens, layouts, slots, block variants, and safe CSS variables.

## Templates

Templates are exportable/importable packages containing:

- page builder documents;
- demo content;
- media references;
- theme token preset;
- required modules;
- import manifest.

## Forbidden

No arbitrary JS in v1 themes. No unsafe CSS injection without sanitization and review.
