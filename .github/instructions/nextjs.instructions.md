---
applyTo: "apps/web/**/*.{ts,tsx},packages/**/*.{ts,tsx}"
---

# Next.js and TypeScript Instructions

Use Next.js App Router patterns. Prefer server components by default. Use client components only for interactivity. Keep TypeScript strict and avoid `any` unless justified with a comment and follow-up task.
