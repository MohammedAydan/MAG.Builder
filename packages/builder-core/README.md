# @nexpress/builder-core

Typed builder kernel for NexPress.

Phase 10 owns:

- versioned builder document schemas
- runtime validation and migrations
- centralized typed block registry
- safe server rendering
- minimal core public blocks
