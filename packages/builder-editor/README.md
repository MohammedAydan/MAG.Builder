# @nexpress/builder-editor

Visual editor adapter for NexPress.

Phase 11 owns:

- Puck integration as an editor-only adapter
- conversion between Puck data and `@nexpress/builder-core` documents
- typed Puck config for current core blocks
- client editor shell with draft save/autosave hooks
